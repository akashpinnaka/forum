<%= form_tag(:action => "attempt_login") do  %>

	<%= label_tag(:registered_id) %><br />
	<%= text_field_tag(:registered_id) %><br />
	<%= label_tag(:password) %><br />
	<%= password_field_tag(:password) %><br />
	<%= submit_tag("Login") %>

<% end %>