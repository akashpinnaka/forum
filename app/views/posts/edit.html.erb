<%= form_for(:post, :url => {:action => "update", :id => @post.id}) do |f| %>

	<%= f.label(:title) %>
	<br />
	<%= f.text_field(:title) %>
	<br />
	<%= f.label(:content) %>
	<br />
	<%= f.text_area(:content) %>
	<br />
	<%= f.submit "Update post", :data => {:disable_with => "Updating your post....."} %>
<% end %>
